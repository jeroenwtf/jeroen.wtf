---
import { Image } from "astro:assets";

interface Props {
	label: string;
	url: string;
}

const { label, url } = Astro.props;
---

<a
	href={url}
	target="_blank"
	class="border block px-4 py-2 text-center rounded-lg hover:bg-zinc-100 dark:border-zinc-700 dark:hover:bg-zinc-800"
	>{label}</a
>
