---
const { subtitle } = Astro.props;
---

<div class="flex flex-col gap-4 mb-10 sm:mb-16">
  <h1 class="font-display font-semibold text-3xl sm:text-4xl leading-snug">
    <slot />
  </h1>

  {subtitle && <p class="text-gray-500 dark:text-gray-400">{subtitle}</p>}
</div>
